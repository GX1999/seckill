<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gx.mapper.UserMapper">  <!-- 除了plus提供的自动查询语句外，还可以在本文件里自定义查询语句，通过id对应UserMapper中的函数 -->

    <!-- 通用查询映射结果（通过逆向工程生成的代码） -->
    <resultMap id="BaseResultMap" type="com.gx.pojo.User">
        <id column="id" property="id" />
        <result column="nickname" property="nickname" />
        <result column="password" property="password" />
        <result column="salt" property="salt" />
        <result column="head" property="head" />
        <result column="register_date" property="registerDate" />
        <result column="last_login_date" property="lastLoginDate" />
        <result column="login_count" property="loginCount" />
    </resultMap>

    <!-- 通用查询结果列（通过逆向工程生成的代码） -->
    <sql id="Base_Column_List">
        id, nickname, password, salt, head, register_date, last_login_date, login_count
    </sql>

</mapper>
